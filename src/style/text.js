import {StyleSheet} from 'react-native';

const text = StyleSheet.create({
    listText: {
        alignSelf: 'flex-start',
        marginLeft: 5,
        fontSize: 16
    },
    defaultText: {
        fontWeight: '400',
        fontSize: 14,
        alignSelf: 'flex-start',
        marginLeft: 5,
    }
});

export default text;
