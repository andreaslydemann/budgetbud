import {StyleSheet} from 'react-native';

export const spinner = StyleSheet.create({
    dialogStandard: {
        position: 'absolute',
        alignItems: 'center',
        justifyContent: 'center',
        top: 63,
    }
});